import { HeroContent } from "@/components/features/hero-content"

export function HeroSection() {
  return (
    <section className="relative w-full overflow-hidden bg-black text-white -mt-16">
      <HeroContent />
    </section>
  )
}
